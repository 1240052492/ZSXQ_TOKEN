# New API + AI 超级画布整合项目

## 模块验收目标与放行基线

文档用途：作为 `QuantumNous/new-api` 与 `1240052492/LocalMiniDrama` 整合项目的验收规格、测试用例索引和发布门槛。

当前状态：**待实现，未开始验收**。文档中的“通过条件”是目标，不表示当前系统已经满足。

适用决策：

- 额度体系沿用 New API（`1A`）。
- 使用一次性 SSO 授权码（`2A`）。
- 画布通过 New API 受控代理调用模型（`3A`）。
- LocalMiniDrama 采用独立私有派生仓维护（`4A`）。

## 1. 统一验收规则

### 1.1 证据要求

每个验收项必须记录：

- 验收编号、版本/commit、环境和执行时间；
- 前置数据、操作步骤、预期结果和实际结果；
- 请求 ID/任务 ID/账务流水 ID；
- 日志、数据库查询、接口响应或截图等可复核证据；
- 缺陷编号及处理结论。

代码存在、接口返回 HTTP 200、单元测试通过，都不能单独证明业务验收通过。

### 1.2 通过门槛

- P0/P1 缺陷为 0；P2 缺陷必须有明确的延期批准和风险记录。
- SSO、钱包、预扣/结算/退款、权限隔离、模型和价格固化用例必须 100% 通过。
- 任一模块的阻断条件命中时，禁止进入下一阶段。
- 生产放行前必须完成备份恢复、异常退款、渠道故障、内容审核和许可证专项审查。

优先级定义：`P0` 表示资金、越权、数据泄露或不可回滚问题；`P1` 表示核心业务不可用或结果不一致；`P2` 表示非核心功能或体验问题。

## 2. 模块验收目标

### M01 项目基线、代码来源与许可证

**目标**：固定两个上游项目的 commit 和依赖边界，LocalMiniDrama 以独立私有派生仓或受控子模块维护，完成 New API AGPL-3.0 及第三方依赖审查。

**通过条件**：

- 记录 New API、LocalMiniDrama、适配层和部署配置的准确 commit；
- 记录修改文件、上游同步策略和发布责任边界；
- 生成依赖许可证清单；
- 对 AGPL-3.0 网络服务、修改分发、源代码提供义务和闭源业务代码边界形成书面结论；
- 未通过合规审查前，生产公网开关保持关闭。

**证据**：commit 清单、SBOM/许可证清单、法律审查结论、仓库权限和发布流程记录。

**阻断条件**：无法追溯构建来源、许可证结论缺失、或派生代码与上游边界不清。

### M02 域名、HTTPS 与服务路由

**目标**：使用两个子域名形成清晰入口：`token.<domain>` 承载 New API，`canvas.<domain>` 承载画布。

**通过条件**：

- `token.<domain>` 可访问首页、注册、登录、模型、钱包和管理入口；
- `canvas.<domain>` 可访问画布业务，未登录用户可被引导至 New API 登录；
- 两个域名证书、SNI、反向代理和健康检查均正确；
- 生产路由不依赖开发端口或 hosts 文件。

**证据**：公网 DNS/HTTPS 检查、代理配置、证书链、健康检查结果和浏览器端到端记录。

**阻断条件**：证书主机名不匹配、公共网络不可达、或路由把画布请求错误转入独立登录/钱包。

### M03 New API 统一注册、登录与账号状态

**目标**：所有平台用户只通过 New API 注册和登录，画布不建立第二套账号入口。

**通过条件**：

- 注册、登录、登出、密码重置、账号禁用均由 New API 完成；
- 画布无独立注册和密码存储；
- 禁用、删除或风控冻结用户无法继续创建画布任务；
- 用户主键映射可审计且不可被普通用户修改。

**关键用例**：新用户注册后进入画布；已登录用户跨子域进入画布；禁用用户访问画布；重复回调不创建重复账号。

**证据**：端到端测试、账号状态日志、画布服务数据库中的用户映射记录。

### M04 一次性 SSO 授权码

**目标**：通过一次性、短有效期、限定 audience 的授权码实现新标签页免登录，不共享跨子域 Cookie。

**通过条件**：

- New API 签发包含 `audience=canvas`、过期时间、随机 `state` 和唯一 ID 的授权码；
- 画布后端兑换授权码并建立自己的会话；
- 授权码使用一次后立即失效；
- 过期、重放、错误 audience、错误 state、伪造码均拒绝；
- 授权码和服务凭证不出现在浏览器日志、URL 长期历史或前端源码中。

**关键用例**：正常兑换、刷新页面重放、并发兑换、跨环境兑换、跨服务兑换。

**证据**：认证服务日志、兑换接口响应、重放测试报告、浏览器网络记录和安全扫描。

**阻断条件**：可重放、可跨 audience 使用、画布直接验证用户密码、或将长期 bearer token 放入 URL。

### M05 三库三账号与数据隔离

**目标**：同一 PostgreSQL 集群内使用 `new_api`、`opc_core`、`super_canvas` 分库、分账号、分迁移，不共享业务表。

**通过条件**：

- 三个数据库拥有独立 owner、runtime 账号和 migration 账号；
- `super_canvas` 和 `opc_core` 无钱包表写权限；
- 不存在跨库业务外键、共享 ORM Model 或直接跨库 SQL；
- 服务间只通过内部 API 和稳定 ID 交换数据；
- 三库可独立迁移、备份和恢复。

**证据**：数据库角色权限导出、migration 目录、访问审计、架构扫描和恢复演练。

**阻断条件**：画布能直接更新余额、跨库写业务表、或单次迁移必须同时修改三个服务的业务表。

### M06 充值、钱包与统一账务

**目标**：画布、API 和后续业务共用 New API 钱包，其他服务只提交业务事件和明细，不直接扣款。

**通过条件**：

- 充值订单、支付回调、余额流水和对账归属于 New API；
- 支付回调具备幂等键，重复回调不重复入账；
- 账务流水可关联用户、订单、业务来源、任务和请求；
- 画布服务账号不能写余额或流水；
- 余额变化可由流水和审计记录解释。

**关键用例**：正常充值、重复回调、乱序回调、支付失败、订单超时、并发余额变化、对账差异。

**证据**：订单和流水、幂等测试、数据库权限拒绝结果、对账报告。

**阻断条件**：余额被业务服务直接修改、重复入账、或无法从流水还原余额变化。

### M07 预扣、实际结算、失败退款

**目标**：采用“预扣—实际结算—失败退款”规则，且全流程幂等、可审计。

**通过条件**：

- 任务创建前取得报价和价格版本；
- 创建预扣记录并返回唯一 `reservation_id`；
- 成功任务只结算一次，按约定的固定档位或实际用量结算；
- 执行前失败、无有效结果失败、超时和审核拒绝按规则释放/退款；
- 工作流多节点可分别结算，成功节点不因失败节点被错误冲销；
- 重试、回调和补偿操作不会重复扣款或重复退款。

**关键用例**：成功、模型调用失败、渠道失败、超时、用户取消、审核失败、服务重启后恢复。

**证据**：余额前后快照、预扣/结算/退款流水、任务状态机、幂等测试报告。

**阻断条件**：资金无法对账、失败任务仍扣全款、或同一任务出现两次结算。

### M08 模型主数据、启用状态与能力目录

**目标**：模型目录由 New API 维护；画布只展示同时满足启用、画布开关、任务能力和协议适配条件的模型。

**通过条件**：

- 每个平台模型有稳定别名、启用状态、能力标签和协议适配器；
- 能力覆盖文本、图像、视频、语音、音乐、理解/审核；
- `enabled=false` 或 `canvas_enabled=false` 的模型不出现在画布工具；
- 不匹配任务能力、参数档位或协议的模型不可被前端绕过目录直接调用；
- 模型目录接口不返回供应商密钥。

**关键用例**：逐项切换启用状态、能力标签、协议适配器和任务类型，验证正向展示与反向隐藏。

**证据**：后台模型配置、目录 API 响应、画布工具栏截图、代理侧二次校验日志。

### M09 渠道容灾与模型不静默替换

**目标**：同一平台模型可配置多渠道容灾，但不得无提示替换不同模型或不同价格规则。

**通过条件**：

- 渠道绑定明确的 `platform_model_id`、能力和价格版本；
- 主渠道故障时只切换到同一平台模型且符合协议的备用渠道；
- 不同模型、不同能力或不同价格不能被自动替换；
- 所选渠道和实际模型写入任务快照、账务流水和审计日志；
- 无合规渠道时任务失败并按规则退款。

**关键用例**：主渠道故障、备用渠道成功、备用渠道价格不一致、所有渠道故障、仅其他模型可用。

**证据**：故障演练记录、实际渠道 ID、任务快照、价格校验和退款流水。

**阻断条件**：用户不知情地切换模型或价格，或渠道失败后绕过 New API 直连供应商。

### M10 价格草稿、发布和任务价格固化

**目标**：管理员以“平台模型别名 + 能力 + 参数档位”配置积分规则，草稿不生效，发布后生效，任务创建时固化价格版本。

**通过条件**：

- 价格具有 `draft`、`published`、`retired` 状态和唯一版本；
- 草稿不会参与报价或任务创建；
- 发布只影响新任务；
- 任务快照保存模型、能力、参数、报价、价格版本和渠道；
- 运行中任务和历史账单不因管理员改价变化；
- 停用版本仍可查询历史账单但不能创建新任务。

**关键用例**：草稿报价、发布报价、发布后改价、停用版本、运行中任务结算。

**证据**：价格版本记录、报价响应、任务快照、前后账单对照。

### M11 New API 受控模型代理

**目标**：画布只调用 New API 内部模型代理；供应商密钥、渠道选择和原始扣款逻辑不暴露给画布或浏览器。

**通过条件**：

- 代理接口校验用户、模型、能力、价格版本、预扣和权限；
- 浏览器只能调用画布后端，不持有供应商密钥；
- 画布后端不能绕过代理访问供应商；
- 代理记录请求、响应摘要、实际用量、渠道和任务 ID；
- 超时、重试和上游错误能映射到统一任务状态。

**证据**：网络出口策略、密钥扫描、代理日志、服务账号权限和异常请求测试。

### M12 LocalMiniDrama 画布适配

**目标**：保留 LocalMiniDrama 的编辑器、工作流、节点、任务和素材能力，统一接入 New API 身份、模型和钱包。

**通过条件**：

- 画布登录后只使用 New API 用户 ID；
- 画布不保留独立注册、钱包、供应商密钥和直接扣款路径；
- 节点创建、运行、重试、取消和结果展示正常；
- 节点能力与模型目录过滤一致；
- 画布服务异常不会破坏 New API 钱包账务。

**证据**：画布回归测试、依赖和配置扫描、真实任务链路记录。

### M13 任务状态、幂等与可恢复性

**目标**：任务具备明确状态机，支持重试和服务恢复，不产生重复执行或重复扣款。

**通过条件**：

- 至少覆盖 `created`、`reserved`、`queued`、`running`、`succeeded`、`failed`、`cancelled`、`refunded`；
- 非法状态迁移被拒绝；
- 创建、回调、取消和结算均有幂等键；
- worker 重启后任务可恢复或进入可审计的失败/退款状态；
- 每个节点和工作流的状态、错误原因和价格版本可查询。

**证据**：状态机测试、重复请求测试、故障恢复演练、任务和账务关联查询。

### M14 本地素材、容量、保留期与延迟清理

**目标**：首期本地存储可用于开发和内测，并具备后续对象存储迁移所需的抽象和生命周期能力。

**通过条件**：

- 素材元数据与对象路径分离，归属用户和项目可审计；
- 容量限制、保留期和延迟清理策略生效；
- 任务失败、用户删除和过期素材不会留下可访问孤儿对象；
- 签名 URL 有效期、权限和撤销行为正确；
- 清理任务可重试且不误删未到期素材。

**关键用例**：上传、下载、超容量、到期清理、删除后访问、清理任务重试。

**证据**：文件系统/对象清单、素材表、清理日志、访问控制测试。

### M15 COS/OSS/S3/MinIO/CDN 存储演进

**目标**：正式上线前可切换到管理员配置的 COS、OSS、S3/MinIO 和 CDN，配置不暴露给用户。

**通过条件**：

- 对象存储通过统一 provider 接口接入；
- 管理后台可配置 provider、桶、区域、保留期和 CDN 参数；
- 用户只能获得受控访问 URL，不可读取密钥；
- 切换期间旧素材可读，新素材按明确策略写入；
- 删除、撤销和缓存失效在源站和 CDN 均生效。

**证据**：适配器契约测试、管理员配置审计、密钥泄露扫描、回源/缓存验证。

### M16 ZIP 导出

**目标**：用户可以异步导出自己有权访问的素材，导出任务不阻塞画布和模型任务。

**通过条件**：

- 导出范围经过用户和项目权限校验；
- 大文件使用异步任务，支持进度、失败重试和过期清理；
- ZIP 内容完整、路径安全、无越权文件；
- 下载链接具备短有效期并记录审计。

**证据**：小文件、大文件、混合失败、越权路径和下载完整性测试。

### M17 个人空间与只读分享

**目标**：首期只提供个人空间和只读分享，不做团队协作。

**通过条件**：

- 项目和素材只能被所有者或有效分享授权读取；
- 分享可设置有效期、撤销和访问限流；
- 分享者和访问者均不能编辑、执行任务、复制到他人空间或修改权限；
- 删除项目/素材后分享立即失效；
- 不存在团队成员、共享钱包或隐式组织权限。

**证据**：所有者、匿名访问、过期、撤销、删除后访问和编辑拒绝测试。

### M18 内容审核、违规处理与用户协议

**目标**：首版覆盖提示词、上传素材、任务输入、生成结果和分享前审核，并保留用户协议和处理审计。

**通过条件**：

- 用户首次使用画布前必须确认当前协议版本；
- 审核命中可拒绝、隔离或删除，处理原因、规则版本、时间和操作人可追溯；
- 审核拒绝且未执行的任务释放预扣；
- 违规素材不会继续被分享或通过 CDN 提供；
- 协议更新后可要求重新确认。

**证据**：审核命中/误报/复核测试、违规处理日志、协议版本记录和相关账务流水。

**阻断条件**：审核绕过、违规素材仍可公开访问、或审核失败导致错误扣款。

### M19 安全、隐私与服务间权限

**目标**：实现最小权限、服务认证、防重放、CSRF、防越权、限流、签名 URL 和敏感信息保护。

**通过条件**：

- 服务账号只具备所需数据库/API 权限；
- SSO、内部接口和管理接口均有服务认证与审计；
- 用户无法访问其他用户的任务、素材、流水和分享管理；
- 日志中不出现密码、支付密钥、供应商密钥和完整授权码；
- 关键接口具备限流和异常告警。

**证据**：权限矩阵、渗透/安全测试、日志脱敏扫描、越权测试和告警记录。

### M20 可观测性、备份恢复与发布回滚

**目标**：关键链路可追踪，三库和素材可恢复，服务可独立发布和回滚。

**通过条件**：

- SSO、报价、预扣、代理、结算、退款、审核和分享可按 request/task ID 串联；
- 三库、配置和素材元数据有备份策略和恢复点目标；
- 恢复后钱包流水、任务状态和素材引用一致；
- New API、`opc_core`、`super_canvas` 可独立发布/回滚；
- migration 失败可恢复，不破坏历史账务。

**证据**：链路日志、指标告警、备份清单、恢复演练、发布和回滚记录。

## 3. 端到端验收场景

### E2E-01 正常画布任务

1. 用户在 `token.<domain>` 注册并登录。
2. 新标签页进入 `canvas.<domain>`，完成一次性 SSO。
3. 选择与任务能力匹配的模型和参数档位。
4. 获取已发布价格，创建任务并完成预扣。
5. New API 代理调用模型，画布展示进度和结果。
6. 任务成功后完成结算并保存素材。

必须能关联：用户 ID、任务 ID、预扣 ID、账务流水 ID、模型别名、价格版本、实际渠道和素材 ID。

### E2E-02 失败与退款

分别注入代理前失败、上游错误、超时、用户取消、审核拒绝和 worker 重启。验证任务最终状态、预扣释放/退款、账务幂等和素材清理。

### E2E-03 渠道故障

主渠道故障时验证同平台备用渠道；备用渠道价格或能力不匹配时必须拒绝切换；所有合规渠道不可用时任务失败并退款。

### E2E-04 价格版本冻结

创建任务后发布新价格，验证运行中任务仍使用旧价格版本，新的任务使用新版本，历史账单不变。

### E2E-05 分享、删除与审核

创建素材、生成只读分享、触发审核、撤销分享、删除素材，验证审核结果、访问权限、签名 URL、CDN/对象清理和审计记录。

## 4. 阶段放行门槛

### G0 源码与合规审计

必须通过 M01、M02 的设计审查，并冻结上游 commit、许可证结论、三库边界和接口契约。

### G1 平台核心

必须通过 M03-M11 的 P0/P1 用例，尤其是 SSO、钱包、预扣/结算/退款、目录过滤、价格版本和代理权限。

### G2 画布接入

必须通过 M12-M13 及 E2E-01 至 E2E-04；画布不得保留独立扣款、供应商直连或绕过目录的调用路径。

### G3 内容与素材

必须通过 M14-M19 和 E2E-05；审核、删除、分享撤销和权限隔离无 P0/P1 缺陷。

### G4 生产发布

必须通过 M20，完成真实支付/退款演练、渠道故障演练、备份恢复、公共 HTTPS 验证、正式存储/CDN 切换验证和 AGPL-3.0 专项合规审查。

## 5. 需求追踪矩阵

| 原始需求 | 对应验收模块 |
|---|---|
| 1. 统一 New API 注册/登录 | M03、M04、E2E-01 |
| 2. 统一充值及扣款规则 | M06、M07 |
| 3. `token`/`canvas` 子域名 | M02、M04 |
| 4. 启用模型 + `canvas_enabled` + 能力标签 | M08、M09、M10、M11 |
| 5. 同一 New API 钱包，业务服务不直接扣款 | M05、M06、M07、M11 |
| 6. 按文本/图像/视频/语音/音乐/理解审核展示 | M08、M12 |
| 7. 同模型多渠道容灾，不静默换模型/价格 | M09 |
| 8. 本地素材、对象存储预留、保留期/容量/清理/ZIP | M14、M15、M16 |
| 9. 个人空间只读分享，不做协作 | M17 |
| 10. 预扣、结算、失败退款 | M07 |
| 11. 审核、违规处理、删除、协议提示 | M18 |
| 12. 同 PostgreSQL 集群三库三账号三迁移 | M05、M20 |
| 13. 独立私有派生仓与 AGPL 审查 | M01 |
| 14. 一次性 SSO 授权码免登录 | M04 |
| 15. 内测本地、上线前 COS/OSS/CDN、管理员配置 | M14、M15 |
| 16. 草稿/发布价格和任务价格固化 | M10 |

## 6. 验收记录模板

```text
验收编号：
模块：
版本/commit：
环境：
前置数据：
执行步骤：
预期结果：
实际结果：
请求/任务/流水 ID：
证据链接或附件：
缺陷编号：
结论：通过 / 不通过 / 阻塞
执行人：
复核人：
日期：
```
